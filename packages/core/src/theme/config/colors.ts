import { blackColorsPalette, brandColorsPalette, royalPinkColor, whiteColor } from '../../colors';

export const colorsConfig = {
  brand: brandColorsPalette,
  black: blackColorsPalette,
  'royal-pink': royalPinkColor,
  white: whiteColor,
} as const;
